name: Retired Activation assertion env helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. CAPPO assertion environment contract is committed and must be verified by canonical CI."
