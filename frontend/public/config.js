// Runtime config for the Veklom Workspace.
// Leave window.__VEKLOM_API_BASE__ empty to talk to the same origin (the
// FastAPI backend that serves this bundle at /workspace). Override on a
// per-deployment basis without rebuilding when needed.
window.__VEKLOM_API_BASE__ = "";
